// Core types for the grid layout system
// This file will contain all TypeScript interfaces and types

// Placeholder for grid-related types
export interface GridItem {
  id: string
  // More types will be added when implementing the core functionality
}

export interface GridLayout {
  // Grid layout configuration types will be defined here
}

export interface GridOptions {
  // Grid options and configuration types will be defined here
} 
