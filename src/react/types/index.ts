// React-specific types for the grid layout system
// This file will contain React-specific interfaces and types

import { ReactNode } from 'react'

// Placeholder React-specific types
export interface GridContainerProps {
  children?: ReactNode
  // Additional props will be defined during implementation
}

export interface GridItemProps {
  children?: ReactNode
  id: string
  // Additional props will be defined during implementation
} 
